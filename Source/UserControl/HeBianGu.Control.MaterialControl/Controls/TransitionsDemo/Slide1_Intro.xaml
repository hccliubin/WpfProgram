<UserControl x:Class="HeBianGu.Controls.MaterialControl.Slide1_Intro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HeBianGu.Controls.MaterialControl"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             >
    <local:ColorZone Mode="Light" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <StackPanel VerticalAlignment="Center">
            <TextBlock>Welcome to the transitions showcase!  Take the red pill to begin...</TextBlock>

            <!-- you can easily use the move next, previous, first and last commands -->
            <Button Style="{DynamicResource MaterialDesignFloatingActionButton}" Margin="64"
                Command="{x:Static local:Transitioner.MoveNextCommand}">
                <local:PackIcon Kind="Pill" />
            </Button>
        </StackPanel>
    </local:ColorZone>
</UserControl>
